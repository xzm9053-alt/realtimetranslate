## 实时翻译 v0.1.0

首个公开版本。Android 平台上的**实时字幕 / 屏幕取词翻译**工具，语音识别与 OCR 全部端上离线运行，不需要联网也能识别。

### 主要功能

**🎧 同传字幕**
- 捕获系统内放音频（媒体音 / 麦克风 / 两者），实时识别并翻译成悬浮字幕
- 本地 SenseVoice 模型离线识别（首次启动自动解压，约 230MB）
- 翻译引擎可选：DeepSeek（流式，需 API Key）或微软免费端点（无需 Key）
- 字幕外观可调：字号、背景透明度、原文/译文颜色、双语或单语显示
- 会话结束后可导出为 txt，历史记录支持自动清理或全部保留

**🔍 屏幕取词翻译**
- 框选屏幕上任意区域，实时 OCR 识别其中的文字并翻译到悬浮气泡
- 支持自动 / 英语 / 中英混合 / 日文 / 韩文 OCR 语言选型
- 悬浮球贴边可拖动换边，空闲时折叠成半球，不挡内容
- 可在屏幕上显示淡色外框标出正在翻译的区域

### 本次修复

- **修复屏幕取词转横屏时会话直接结束的问题**：竖屏框选后把手机转成横屏（例如去看视频），之前会因选区超出新屏幕边界而导致取词会话被强制结束（悬浮球消失）。现在改为把选区按旋转角度换算到新坐标系并钳制在屏幕内，**旋转不再中断会话**；旋转后点悬浮球可重新框选区域。
- 顺带加固：录屏管线重建改为原子操作，旋转/折叠屏切换时不再出现尺寸错位的崩溃；启动阶段的异常也做了兜底，避免进程被带走。

### 安装

下载下方 `LiveTranslate-release.apk` 安装即可（使用 debug 签名，需在系统设置中允许「安装未知来源应用」）。

> 首次启动同传字幕时会请求录屏授权，用于捕获其它 App 播放的声音；屏幕取词同样需要录屏授权。

### 已知限制

- 屏幕取词在旋转后选区位置为近似换算，如需精确区域请点悬浮球重新框选
- 微软免费翻译端点偶有限流，失灵时请切换到 DeepSeek
